// Dylan Julian Gonzalez

function mostrar() {
  let edad = txtIdEdad.value;

  if (edad >= 18) {
    return alert(`Usted tiene ${edad} y es mayor de edad `);
  } else if (edad < 18) {
    return alert(`Usted tiene ${edad} y es menor de edad `);
  }
} //FIN DE LA FUNCIÓN
